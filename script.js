let add = (x, y) => {
    return Number(x) + Number(y);
};

let subtract = (x, y) => {
    return Number(x) - Number(y);
};

let multiply = (x, y) => {
    return Number(x) * Number(y);
};

let divide = (x, y) => {
    return Number(x) / Number(y);
};

console.log(add(1,2))